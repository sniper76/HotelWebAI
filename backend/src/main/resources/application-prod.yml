spring:
  datasource:
    url: jdbc:postgresql://prod-db-host:5432/hotel_db
    username: prod_user
    password: prod_password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
logging:
  level:
    com.hotel: WARN
